<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.group3.is.prm391.ailatrieuphu_nhom3">

    <application
            android:name=".ui.App"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".ui.activities.MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.activities.InsertNameActivity">
        </activity>
        <activity android:name=".ui.activities.GameActivity">
        </activity>
        <activity android:name=".ui.activities.WinActivity">
        </activity>
        <activity android:name=".ui.activities.LoseActivity">
        </activity>
        <activity android:name=".ui.activities.RankingActivity">
        </activity>
    </application>

</manifest>